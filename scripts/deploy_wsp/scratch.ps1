. ".\deploy_wsp.ps1"

# $folder = "C:\Users\pdemro\Desktop\deployment"

# #Get-ChildItem $folder

# $latest = Get-ChildItem $folder | Where-Object { $_.PSIsContainer } | Sort-Object CreationTime -desc | Select-Object -f 1

# $latest.FullName

# Get-ChildItem $latest.FullName